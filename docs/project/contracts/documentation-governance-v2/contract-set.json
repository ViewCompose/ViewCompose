{
  "schemaVersion": 1,
  "governanceVersion": 2,
  "status": "strict",
  "recordPlacement": "docs/project/records/documentation-governance-v2",
  "scannerImplementation": "tools/viewcompose-quality-build:verifyDocumentationGovernanceV2",
  "reportOutput": "build/reports/viewcompose-quality/documentation-governance-v2-report.json",
  "humanReportOutput": "build/reports/viewcompose-quality/documentation-governance-v2-report.txt",
  "contracts": [
    {
      "id": "capability",
      "schema": "capability.schema.json",
      "accepted": ["fixtures/accepted/capability.json"],
      "rejected": [
        {
          "fixture": "fixtures/rejected/capability-missing-reference.json",
          "rule": "reference_owner is required"
        },
        {
          "fixture": "fixtures/rejected/capability-exception-owner.json",
          "rule": "sample_owner must resolve directly to a compiled sample"
        }
      ],
      "reviewerOwned": ["capability grouping is cohesive", "document owners are appropriate for user need"]
    },
    {
      "id": "document",
      "schema": "document.schema.json",
      "accepted": ["fixtures/accepted/document.json"],
      "rejected": [
        {
          "fixture": "fixtures/rejected/tutorial-version-agnostic.json",
          "rule": "tutorials use released or next and own a compiled sample"
        }
      ],
      "reviewerOwned": ["the page has one primary purpose", "required evidence supports its claims"]
    },
    {
      "id": "sample",
      "schema": "sample.schema.json",
      "accepted": ["fixtures/accepted/sample.json"],
      "rejected": [
        {
          "fixture": "fixtures/rejected/compiled-sample-missing-target.json",
          "rule": "compiled-region requires a build_target"
        }
      ],
      "reviewerOwned": ["the sample is complete for its intended audience", "non-executable rationale is honest"]
    },
    {
      "id": "capability-impact",
      "schema": "capability-impact.schema.json",
      "accepted": ["fixtures/accepted/capability-impact.json"],
      "rejected": [
        {
          "fixture": "fixtures/rejected/moved-capability-without-redirect.json",
          "rule": "moved or deleted public ownership requires a redirect disposition"
        }
      ],
      "reviewerOwned": ["Q level and contract fields are complete", "not-applicable rationales are correct"]
    },
    {
      "id": "exception",
      "schema": "exception.schema.json",
      "accepted": [],
      "rejected": [
        {
          "fixture": "fixtures/rejected/wildcard-exception.json",
          "rule": "exception targets must be exact and cannot contain wildcards"
        }
      ],
      "reviewerOwned": []
    }
  ],
  "enforcement": {
    "mode": "blocking strict verification",
    "issues": "every discovered issue blocks",
    "exceptions": "all exception records are forbidden",
    "publicCapabilityImpact": "every detected public capability change requires one newly added exact immutable impact record"
  }
}
