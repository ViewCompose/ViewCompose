{
  "schemaVersion": 1,
  "summary": "Hard-cut connected-device preflight and benchmark/report tooling into compiled quality build ownership without changing published artifacts or task selection.",
  "shared": [
    {
      "path": "build.gradle.kts",
      "reason": "Remove repository-only device process control and benchmark/report command registrations after compiled parity acceptance while preserving stable task names and lifecycle wiring."
    }
  ]
}
