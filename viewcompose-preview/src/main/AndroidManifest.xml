<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <receiver
            android:name=".device.DeviceDslSourceRequestReceiver"
            android:exported="true"
            android:permission="android.permission.DUMP">
            <intent-filter>
                <action android:name="com.viewcompose.preview.action.REQUEST_DEVICE_DSL_SOURCE" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
