package com.viewcompose.runtime

/** Identifies the platform-neutral ViewCompose runtime artifact. */
object UiRuntime {
    /** Stable runtime identifier used by diagnostics and integration tooling. */
    const val NAME: String = "ui-runtime"
}
