package com.viewcompose.runtime

/**
 * 运行时入口标记，覆盖状态、观察以及后续调度器能力。
 * Runtime entry marker for state, observation, and future scheduler APIs.
 */
object UiRuntime {
    const val NAME: String = "ui-runtime"
}
