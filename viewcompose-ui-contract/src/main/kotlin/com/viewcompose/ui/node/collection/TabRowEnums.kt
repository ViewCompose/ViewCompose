package com.viewcompose.ui.node.collection

/** Places a tab indicator at the physical top or bottom edge of a tab row. */
enum class TabIndicatorPosition { Top, Bottom }

/** Selects item-matched width or an explicit fixed width for a tab indicator. */
enum class TabIndicatorWidthMode { MatchItem, Fixed }
