package com.viewcompose.ui.node

/**
 * 图片内容在目标区域中的缩放策略。
 * Scaling strategy for image content inside its target bounds.
 */
enum class ImageContentScale {
    Fit,
    Crop,
    FillBounds,
    Inside,
}
